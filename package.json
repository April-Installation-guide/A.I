{
  "name": "mancy-bot",
  "version": "1.0.0",
  "type": "module",
  "description": "Bot con identidad de Mancy",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "dev": "nodemon bot.js",
    "test": "node -e \"import('./Modules/IdentityCore.js').then(m => { const M = m.default; const mancy = new M(); console.log(mancy.respondToPersonalQuestion('¿Quién eres?')); })\""
  },
  "dependencies": {
    "discord.js": "^14.0.0",
    // Tus otras dependencias...
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
