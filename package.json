{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker:build": "docker build -t mancy-ai .",
    "docker:run": "docker run -p 10000:10000 --env-file .env mancy-ai",
    "docker:compose": "docker-compose up",
    "docker:compose-build": "docker-compose up --build",
    "render:deploy": "docker build -t mancy-ai . && echo 'Listo para subir a Render'"
  }
}